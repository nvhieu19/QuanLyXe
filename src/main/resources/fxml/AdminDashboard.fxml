<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane fx:id="mainPane" minHeight="700.0" minWidth="1400.0" styleClass="admin-dashboard"
            xmlns="http://javafx.com/javafx" xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.fucar.controller.AdminDashboardController">

    <!-- Left Side Navigation Menu -->
    <left>
        <VBox prefWidth="250.0" style="-fx-background-color: #2c3e50;" styleClass="side-menu" spacing="10">
            <padding>
                <Insets top="20" right="10" bottom="20" left="10"/>
            </padding>

            <!-- App Logo and Title -->
            <VBox alignment="CENTER" spacing="10.0" styleClass="side-menu-header">
                <Label text="🚗" styleClass="app-logo-side">
                    <font><Font size="40.0"/></font>
                </Label>
                <Label text="FU Car Renting" style="-fx-text-fill: #ecf0f1;" styleClass="app-title-side">
                    <font><Font name="System Bold" size="18.0"/></font>
                </Label>
            </VBox>

            <Separator prefWidth="200.0"/>

            <!-- Navigation Buttons -->
            <VBox spacing="10.0" VBox.vgrow="NEVER">
                <Button onAction="#loadCarManagement" text="🚘 Quản lý Xe" maxWidth="Infinity" styleClass="nav-button"/>
                <Button onAction="#loadCustomerManagement" text="👥 Quản lý Người dùng" maxWidth="Infinity" styleClass="nav-button"/>
                <Button onAction="#loadRentalManagement" text="📋 Quản lý Giao dịch" maxWidth="Infinity" styleClass="nav-button"/>
                <Button onAction="#loadReport" text="📊 Báo cáo" maxWidth="Infinity" styleClass="nav-button"/>
            </VBox>

            <!-- Spacer -->
            <Region VBox.vgrow="ALWAYS"/>

            <!-- User Info and Logout -->
            <VBox alignment="CENTER" spacing="10.0" styleClass="side-menu-footer" stylesheets="@../css/styles.css">
                <Separator prefWidth="200.0"/>
                <Label fx:id="lblWelcome" text="Welcome, Admin" style="-fx-text-fill: #bdc3c7;" styleClass="welcome-text">
                    <font><Font size="13.0"/></font>
                </Label>
                <Button onAction="#handleLogout" text="ĐĂNG XUẤT" maxWidth="Infinity" styleClass="logout-button">
                    <font><Font name="System Bold" size="13.0"/></font>
                </Button>
            </VBox>
        </VBox>
    </left>

    <center>
        <!-- Content will be loaded here dynamically -->
        <StackPane style="-fx-background-color: #f4f6f9;" styleClass="content-pane">
            <Label text="Chào mừng đến với Bảng điều khiển Quản trị!" style="-fx-text-fill: #7f8c8d;" styleClass="placeholder-text">
                <font>
                    <Font name="System Italic" size="24.0"/>
                </font>
            </Label>
        </StackPane>
    </center>
</BorderPane>